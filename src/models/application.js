export default class Application {
    constructor(id, description, gameId, videoApplication, status) {
        this.id = id;
        this.description = description;
        this.gameId = gameId;
        this.videoApplication = videoApplication;
        this.status = status;
    }
}